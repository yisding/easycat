# Copy this file to .env, then replace only the values your chosen mode needs.
# Never commit .env or paste real credentials into shell commands.

# Required by the default VoiceApp local quickstart.
OPENAI_API_KEY=

# Optional provider swaps.
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
CARTESIA_API_KEY=

# Optional browser/Twilio deployment settings are documented by their examples.
