repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.15.8
  hooks:
    # Run the linter.
    - id: ruff-check
      exclude: ^src/easycat/cli/scaffold/templates/
    # Run the formatter.
    - id: ruff-format
      exclude: ^src/easycat/cli/scaffold/templates/
