OPENAI_API_KEY=sk-your-key-here
$EXTRA_ENV_VARS
# Optional: use Deepgram STT by adding the `deepgram` extra in pyproject.toml,
# running `uv sync`, setting `stt="deepgram/flux"` in agent.py, and filling in
# DEEPGRAM_API_KEY here.
