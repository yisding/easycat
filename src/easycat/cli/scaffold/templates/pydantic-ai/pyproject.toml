[project]
name = "$PROJECT_NAME"
version = "0.1.0"
description = "EasyCat voice agent (PydanticAI)."
requires-python = ">=3.11"
dependencies = [
    "easycat[$EXTRAS]>=0.1.0",
]

[tool.ruff]
line-length = 99
target-version = "py311"
