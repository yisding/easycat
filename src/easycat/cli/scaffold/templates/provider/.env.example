OPENAI_API_KEY=sk-your-key-here

# The live demo in agent.py uses the default OpenAI STT/TTS providers; the
# EnergyVAD provider itself needs no credentials. Replace OPENAI_API_KEY
# with your own provider's key once you swap the backend.
