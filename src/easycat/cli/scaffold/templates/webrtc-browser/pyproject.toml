[project]
name = "$PYPROJECT_NAME"
version = "0.1.0"
description = "EasyCat browser voice agent (WebRTC + OpenAI Agents SDK)."
requires-python = ">=3.11"
dependencies = [
    "easycat[$EXTRAS]>=0.1.0",
]

[tool.ruff]
line-length = 99
target-version = "py311"
