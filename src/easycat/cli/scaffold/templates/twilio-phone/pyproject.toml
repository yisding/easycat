[project]
name = "$PYPROJECT_NAME"
version = "0.1.0"
description = "EasyCat Twilio phone agent (Media Streams + OpenAI Agents SDK)."
requires-python = ">=3.11"
dependencies = [
    "easycat[$EXTRAS]>=$EASYCAT_VERSION_FLOOR",
]

[tool.ruff]
line-length = 99
target-version = "py311"
