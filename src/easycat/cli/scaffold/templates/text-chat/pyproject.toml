[project]
name = "$PROJECT_NAME"
version = "0.1.0"
description = "EasyCat text-mode agent (REPL)."
requires-python = ">=3.11"
dependencies = [
    "easycat[openai-agents]>=0.1.0",
]

[tool.ruff]
line-length = 99
target-version = "py311"
