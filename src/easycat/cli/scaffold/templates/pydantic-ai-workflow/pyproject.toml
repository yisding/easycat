[project]
name = "$PYPROJECT_NAME"
version = "0.1.0"
description = "EasyCat voice workflow (PydanticAI specialists)."
requires-python = ">=3.11"
dependencies = [
    "easycat[$EXTRAS]>=$EASYCAT_VERSION_FLOOR",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.9",
]

[tool.ruff]
line-length = 99
target-version = "py311"
