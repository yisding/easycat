OPENAI_API_KEY=sk-your-key-here

# agent.py uses OpenAI for its agent and default STT. ToneTTS is offline.
# LIVE TODO: add YOUR_TTS_API_KEY= when replacing the tone backend, and
# declare the same name in register_tts_provider(env_var=...).
