OPENAI_API_KEY=sk-your-key-here

# agent.py uses OpenAI for its agent and default TTS. ScriptedSTT is offline.
# LIVE TODO: add YOUR_STT_API_KEY= when replacing the scripted backend, and
# declare the same name in register_stt_provider(env_var=...).
