# Testing Plans

Status: historical record index.

Longer-form test strategy documents that do not belong to one feature module.

Current note: the old debug-first E2E planning dump has been removed. Static
inspection found the useful parts represented by concrete tests under
`tests/e2e/`, including latency, interruption, adversarial audio, stress, and
record/replay coverage. Add a new file here only for test strategy that is not
already represented by a current test module or maintained docs route.
