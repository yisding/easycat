# Neo Acceptance Matrix

Status: active review checklist.

Use this matrix during implementation reviews. A milestone is not done until its
behavior, tests, docs, and migration notes are represented here.

## Phase 1 — VoiceApp

| Capability | Acceptance Criteria | Evidence |
|---|---|---|
| Public import | `from easycat import VoiceApp` is lazy and does not import heavy provider SDKs. Top-level `VoiceApp` is the ONLY new symbol against the top-level cap, which is fully exhausted at 94/94, so this PR bumps the `tests/test_public_api.py` cap 94->95 AND lands the triple-lock in the same PR: add `VoiceApp` to `__all__`, to `LAZY_EXPORTS`, to `docs/public-api.md`, and to `PUBLIC_API_SNAPSHOT`. `CostBudget`/`LatencyBudget`/`VoiceServer`/`EvalRunner`/`EvalScenario`/`EvalTurn` are submodule exports (`easycat.budgets`/`easycat.server`/`easycat.evals`) and do NOT count against this cap. | Public API test asserts the cap is now 95 and that the snapshot includes `VoiceApp`; import-cost/lazy import test. |
| Local mode | `VoiceApp(agent=agent).session("local")` builds a local-session config and `run("local")` delegates to `run_session`. | Unit test with monkeypatched `create_session`/`run_session`. |
| Browser mode | `run("browser")` starts WebRTC config server with per-offer session factory. | Unit test with monkeypatched `run_webrtc_config_server`; WebRTC focused test. |
| WebSocket mode | `run("websocket")` starts WebSocket config server with per-client transport. | Unit test with monkeypatched `run_websocket_config_server`. |
| Static config safety | A static transport-bearing `EasyConfig` is NOT safely cloneable: `dataclasses.replace` shares the grouped `observability`/`audio_processing`/`session_policy` sub-configs BY REFERENCE, so mutating a replaced config flips the original. No `with_transport`/`replace_transport`/clone primitive exists in `config/`. Therefore per-connection modes (browser/websocket/twilio) MUST go through `config_factory`; a single static config may only be used for single-session `local` mode. | Concurrency unit test asserts that mutating a `dataclasses.replace`-cloned config's `observability.debug` ALSO flips the original (proving naive cloning does NOT isolate concurrent sessions), and that the per-connection path mandates `config_factory`. |
| Factory mode | `config_factory` is the ONLY safe per-connection mechanism. Canonical signature is the per-transport factory shape `Callable[[TransportT], EasyConfig]` (concrete `WebRTCTransport`/`WebSocketConnectionTransport`/`WebTransportConnectionTransport`/`TwilioConnectionTransport`); the abstract `ConnectionContext` type is deleted from the plan and never referenced. It receives the connection transport and is called once per session. | Unit test asserts the factory is invoked once per session with the concrete transport and that each session gets an isolated `EasyConfig` instance. |
| CLI default | `easycat serve` still defaults to browser mode. | CLI test. |
| CLI security | Non-loopback `easycat serve` still requires token. | CLI test. |
| Twilio mode | `run("twilio")` starts reusable Twilio helper or raises clear optional-extra error. | Telephony fake tests. |
| Docs | README, browser playground docs, examples, and public API docs show `VoiceApp`. | Docs tests / markdown guard. |

## Phase 2 — VoiceServer

| Capability | Acceptance Criteria | Evidence |
|---|---|---|
| Server skeleton | `VoiceServer.from_app(VoiceApp(...))` starts and stops without leaking sessions. Capacity/draining/active-set state is NET-NEW at the shared VoiceServer layer: it is LIFTED out of the per-transport serve helpers (`webrtc.py:354,356-357,377-390,422-434,475-482`; `websocket.py:146,154-156,177-179`), NOT inherited from `SessionManager`, which is a bare add/remove/stop_all registry (`session_manager.py:18-105`) with no `max_sessions`, no `__len__`, and no draining state. `VoiceServer` mounts apps via their `config_factory` ONLY and never calls `VoiceApp.run()`. | `tests/server/test_voice_server_lifecycle.py` asserts no session leak and that the shared layer (not `SessionManager`) owns the active-set/semaphore. |
| Health live | `/health/live` returns 200 while process can respond. | Aiohttp route test. |
| Health ready (M4 scope) | `/health/ready` returns 200 when serving and 503 while draining/at capacity. M4 owns ONLY the serving/draining/capacity/route-ready signals; the manifest-loaded and plan-no-blocking-errors signals are DEFERRED to M6b. | Health route tests covering serving/draining/capacity/route-ready only. |
| Health ready (M6b scope) | `/health/ready` additionally fails (503) when the manifest is not loaded or the provider plan reports blocking errors. These M6b-owned readiness checks are gated behind the planner-parity test below passing. | Health route tests covering manifest-loaded + plan-no-blocking-errors signals. |
| Planner parity (REQUIRED gate, M6b) | The planner verdict matches the `create_session` outcome for every one of the 7 roles (stt/tts/vad/transport/agent/noise_reducer/echo_canceller): a role the planner reports as resolvable actually constructs, and a role it reports as blocked actually fails `create_session`. Reuse is catalog-only for stt/tts; vad/transport/agent/noise/echo use NET-NEW declarative metadata, so divergence surface is large and this test is mandatory. The manifest/plan readiness checks (above) MUST NOT ship until this test passes. | Parity test enumerating all 7 roles and asserting planner verdict == `create_session` outcome. |
| Auth | Bearer token accepted; missing/invalid token rejected; query-token compatibility only when enabled. The shipped env var name is `EASYCAT_SERVE_TOKEN` (not `EASYCAT_SERVER_TOKEN`). | Auth tests. |
| Non-loopback auth guard | The non-loopback-requires-token guard is a property of the unified `VoiceServer`/`AuthPolicy` layer applied to BOTH WebSocket and WebRTC transports. A non-loopback bind (e.g. `0.0.0.0`) with NO token and WITHOUT the explicit `AuthPolicy.unsafe_allow_no_auth=True` escape hatch RAISES at startup. This CLOSES a real unauthenticated `0.0.0.0` voice endpoint on the WebSocket path (`websocket.py:84,93,100-111` has no loopback gate today; WebRTC already enforces it at `webrtc.py:347-351,924-927`); it is not parity preservation. `unsafe_allow_no_auth` is a structured `AuthPolicy` field (default `False`), not prose-only. | Server bind/auth test asserts a non-loopback WebSocket bind with no token and without `unsafe_allow_no_auth` raises, and that setting `unsafe_allow_no_auth=True` allows it. |
| Query-token break note | `allow_query_token` defaults to `False`. `?token=` is UNCONDITIONAL today whenever a token is set (`webrtc.py:826-834`, `websocket.py:102-111`), so `allow_query_token=False` is a BREAKING CHANGE for the bundled WebSocket browser client (`examples/ws_browser_client.html:91-93` — browsers cannot set headers on the WS handshake). The bundled WebRTC client is UNAFFECTED (it sends `Authorization: Bearer`). A `allow_query_token=True` loopback opt-in is provided; the new default applies to the existing handlers too. | Auth test asserts query-token rejected by default and accepted only under the loopback opt-in. |
| Secret hygiene | Tokens do not appear in JSON diagnostics or metrics labels. | Auth/metrics tests. |
| Capacity | New sessions are rejected after max active sessions. The `Semaphore`/active-set that enforces this is net-new at the shared layer (lifted from the serve helpers, not added to `SessionManager`). | Capacity test against the shared VoiceServer internals. |
| Graceful shutdown | Server stops accepting new sessions, drains active sessions, then escalates after timeout. Draining state lives in shared VoiceServer internals; the test must NOT assume `SessionManager` owns draining (it is a bare registry). | Shutdown test with fake sessions asserting the shared layer drives drain->escalate, with `SessionManager` used only as the add/remove/stop_all registry. |
| Manifest loading | `easycat.toml` discovered, parsed, resolved relative to manifest dir, and converted to `EasyConfig`. NO loader exists today (no `bearer-env`/`tomllib`/`ProjectManifest` in tree) — this is net-new. | Project loader tests. |
| Manifest secret rule | `auth`/`token` fields MUST match the env-reference grammar `bearer-env:NAME` (NAME is a user-chosen env var identifier; `bearer-env:EASYCAT_SERVE_TOKEN` is the canonical example). The loader RAISES a coded error (`EASYCAT_Exxx`) when it sees a literal-looking secret (reuse `redaction._SECRET_RE`/`contains_unredacted_sensitive_text`). Any echoed/dumped manifest routes through `redact_value`. | Loader tests: (a) a literal-looking secret in `auth`/`token` is rejected with the coded error; (b) the `--json`/`/manifest` dump shows NO resolved token value (only the env reference). |
| Provider plan | Missing env/extras reported without provider instantiation. STT/TTS reuse `ProviderCatalog` metadata; vad/transport/agent/noise_reducer/echo_canceller use NET-NEW declarative metadata (no static catalog exists for those 5 roles). | Planner tests. |
| CLI plan | `easycat plan --json` emits provider plan envelope (`schema_version=1`). The envelope is covered by `tests/cli/test_json_schema.py`. | CLI JSON test in `tests/cli/test_json_schema.py`. |
| WebRTC route | `VoiceServer` can serve browser WebRTC without breaking old helper APIs. | WebRTC integration tests. |
| Metrics registration | The new `easycat.server.*` metric names are registered in `METRIC_DEFINITIONS` and the new labels `easycat.server_state`/`easycat.auth_result`/`easycat.route` are registered in `LOW_CARDINALITY_ATTRIBUTE_KEYS` in `_observability.py` IN THE SAME PR that emits them, because `_record_metric`/`sanitize_attributes` raise `ValueError` on any unregistered name/key (`:199,209-210`). `easycat.transport` is ALREADY registered (`:59`) and is not re-added. | Metrics test asserts the server emits through `sanitize_attributes` with no `ValueError` (registered names/labels). |
| Metrics route safety | `easycat.route` is constrained to an ENUMERATED set of route TEMPLATES; the value is asserted to be in the template set BEFORE recording, so a raw path with user content can never be emitted as a label. | Metrics test asserts an off-template route value is rejected before recording. |
| `/metrics` endpoint | `GET /metrics` returns 200 and the documented JSON shape. | Aiohttp route + JSON shape test. |
| `/manifest` endpoint | `GET /manifest` returns 200 and the documented JSON shape with NO resolved token values (routed through `redact_value`). | Aiohttp route + JSON shape test. |
| `/plan` endpoint | `GET /plan` returns 200 and the provider-plan JSON shape. | Aiohttp route + JSON shape test. |
| `/capabilities` endpoint | `GET /capabilities` returns 200 and the documented JSON shape. | Aiohttp route + JSON shape test. |
| Secret hygiene (metrics) | Tokens do not appear in JSON diagnostics or metrics labels. | Auth/metrics tests. |
| Docs | Deployment, Docker, observability, and README docs explain `VoiceServer` and manifest. | Guard docs. |

## Phase 3 — Feedback Loop

| Capability | Acceptance Criteria | Evidence |
|---|---|---|
| Dev activation (M13) | `EASYCAT_DEV=1` or `VoiceApp(dev=True)` enables dev debugger defaults: durable journaling by default + a loopback session-selector UI launched ONCE per process via `easycat.debugger.dev.maybe_launch_dev_debugger` / `maybe_launch_dev_registry_ui`. Made testable: assert the dev launch HOOK is INVOKED (mock `_launch_dev_ui`), NOT that a browser literally opens, since CI is non-interactive. | `tests/debugger/test_dev_autolaunch.py::test_dev_opt_in_invokes_launch_hook_once_per_process` + `::test_dev_registry_ui_launch_hook_fires_once`. |
| Dev opt-in is additive (M13) | The new `EASYCAT_DEV`/`VoiceApp(dev=True)` opt-in is PURELY ADDITIVE: it is a SEPARATE trigger (`dev.py`) and never weakens the existing `debug='full'`-alone-never-autolaunches guarantee (`_autolaunch.py:40-51,70`; `tests/test_dx_helpers.py`). | `tests/debugger/test_dev_autolaunch.py::test_debug_full_alone_does_not_invoke_dev_launch` + `::test_autolaunch_guard_still_blocks_debug_full`. |
| No surprise autolaunch | `debug="full"` alone does not open debugger UI. | Existing/new DX test (`tests/test_dx_helpers.py`); `tests/debugger/test_dev_autolaunch.py::test_debug_full_alone_does_not_invoke_dev_launch`. |
| Session registry (M13) | One debugger process lists/switches live sessions via a process-local `session_registry` (`register_session`/`unregister_session`/`list_sessions` -> `LiveSessionSummary`); `GET /api/dev/sessions` lists them and `POST /api/dev/select` re-points every panel at the selected session (single session auto-selects). | `tests/debugger/test_dev_autolaunch.py::test_register_and_list_sessions_round_trips` + `::test_dev_sessions_route_lists_and_selects`. |
| Budget overlays (M13) | `GET /api/budgets` evaluates the active session's configured latency/cost budgets through the shared M9 `build_budget_report` and degrades to an empty passing report (never 500) when none are configured; the UI renders a budget badge from it. | `tests/debugger/test_dev_autolaunch.py::test_budgets_route_uses_shared_report` + `::test_budgets_route_passes_without_budgets`. |
| Dev promote endpoint (M13) | `POST /api/dev/promote` exports the active session and promotes one turn through the M11 hardened `easycat.evals.promote_turn_to_test` — redact-by-default (`redacted=True`), audio excluded by default (`include_audio=False`), hash assertion default — surfacing the generated test/bundle paths; a missing turn id yields 400. | `tests/debugger/test_dev_autolaunch.py::test_dev_promote_route_uses_hardened_path` + `::test_dev_promote_requires_turn_id`. |
| Dev/prod source labeling (M13) | The dev registry routes are mounted ONLY for a registry-backed app (a bundle/session app 404s `/api/dev/*`); the UI source-mode badge distinguishes a live dev registry from a read-only bundle. | `tests/debugger/test_dev_autolaunch.py::test_dev_routes_absent_for_plain_source`; `tests/debugger/test_server_static_assets.py`. |
| Evals package (re-exports) | `easycat.evals` re-exports the PURE re-exports of `easycat.debug.testing`: `load_bundle`, `assert_no_error`, `assert_tool_called`, `assert_regex`, `assert_exact_match`, `assert_latency`, `assert_turn_completed`. These are importable and need no new unit tests beyond the re-export assertion. | Import/public API test asserts every re-export resolves to the `easycat.debug.testing` symbol. |
| Evals package (net-new) | `easycat.evals` also exports NET-NEW symbols that do NOT exist today and each need their own unit tests: `assert_budgets_pass`, `EvalRunner`, `EvalScenario`, `EvalTurn`, `promote_turn_to_test`, `ScenarioResult`. (`assert_budgets_pass` and `promote_turn_to_test` are net-new; today only `assert_latency` with a different signature and the CLI `promote_turn` exist.) | Import/public API test asserts the net-new set is importable AND each net-new symbol has dedicated unit tests. |
| Text scenario | Eval runner executes text turns without live audio. Text scenarios (the only no-API-key mode) emit ONLY `total_ms` (`_turn_runner.py:846-853`), so they assert ONLY turn-total + cost budgets. A provider-stage budget (e.g. `tts_ttfb_ms`, `llm_ttft_ms`) in a text scenario evaluates against ZERO samples; rather than passing vacuously it RAISES a clear "no samples for stage X" error. | Eval runner tests: a text scenario with a turn-total/cost budget passes; a text scenario with a provider-stage budget raises "no samples for stage X". |
| Scenario file loader (M10) | `easycat.evals.load_scenario` parses a `.yaml`/`.yml`/`.json` scenario file into `EvalScenario`, accepting both the nested `expect:` and flat turn shapes and the `budgets.{latency,cost}` block (cost via `CostBudget.from_dict`, so `max_session_cost_usd` round-trips). YAML requires `pyyaml` and raises a clear error when missing. | `tests/evals/test_scenario.py`. |
| Budget assertion helper (M10) | `assert_budgets_pass` accepts either a pre-built `BudgetReport` or a `(records, budgets)` pair, evaluates via `build_budget_report`, returns the report on pass, and raises a violation-listing `AssertionError` on fail. Distinct from `assert_latency` (set A). | `tests/evals/test_assertions.py`. |
| ScenarioResult records compatibility (M10) | `EvalRunner.run` returns a journal-backed `ScenarioResult` whose `.records()` yields the same dict shape as `RunBundle.records()`/`TurnResult.records()`, so the re-exported `assert_*` helpers accept it directly. | `tests/evals/test_runner.py`. |
| CLI eval run | `easycat eval run` executes scenario file(s) (file or directory) and emits a JSON report envelope (`schema_version=1`, `kind="eval_run"`); a failing scenario yields exit code 1 with `status="error"`. Covered by `tests/cli/test_json_schema.py`. | CLI eval tests + `tests/cli/test_json_schema.py`. |
| CLI eval report | `easycat eval report PATH` inspects a saved eval report and emits a JSON report envelope (`schema_version=1`), mirroring `validate report` (`cli/validate.py:676,833`). Covered by `tests/cli/test_json_schema.py`. | CLI eval report test + `tests/cli/test_json_schema.py`. |
| Promotion | `easycat eval promote PATH TURN_ID` generates a pytest skeleton. `eval promote` is a NEW FORKED command, NOT a silent extension of `journal promote`; `journal promote` is documented as the unsafe legacy path and `eval promote` is the hardened replacement (the two verbs are reconciled in the promotion milestone). It reuses `_promote_test_stub`/`_validate_promoted_slice` rather than re-implementing. The `--json` envelope is covered by `tests/cli/test_json_schema.py`. | Promotion tests; the generated `.py` imports and runs under `pytest` in `tmp_path`; `tests/cli/test_json_schema.py` covers the `--json` envelope. |
| Promotion redaction (hardening, not preservation) | The existing path (`journal promote` -> `slice_bundle_by_turn` -> `debug/export.py:154-170`) copies full raw NDJSON + EVERY audio blob + verbatim transcript into a committed file with ZERO redaction; this is HARDENING. Concrete, testable criteria for `eval promote`: (a) `--no-audio` is the DEFAULT (mirror `bundles export` at `bundles.py:1160`); (b) records route through `redact_value` before serialization (redact-by-default); (c) writing unredacted text REQUIRES `--allow-pii`, otherwise the `contains_unredacted_sensitive_text()` tripwire RAISES (mirror `_assert_context_pack_redacted`); (d) record-assertion mode DEFAULTS to asserting on a HASH/REGEX over the reply rather than embedding the raw verbatim transcript. | Promotion tests: default run emits no audio and no unredacted text; the tripwire raises without `--allow-pii`; the generated assertion uses a hash/regex by default; the generated `.py` imports and runs in `tmp_path`. |
| Promotion CLI envelope (M11) | `easycat eval promote PATH TURN_ID --out FILE --json` emits the promotion envelope (`schema_version=1`, `command="eval promote"`) carrying `out`, `bundle`, `turn_id`, `mode`, `assert_on`, `include_audio`, and `redacted`; a missing turn yields exit code 2 with `status="error"`. Covered by `tests/cli/test_json_schema.py` and the `--json`-coverage guard. | `tests/cli/test_json_schema.py::test_eval_promote_envelope` + `::test_eval_promote_error_envelope`; `tests/evals/test_cli.py`. |
| Promotion reply-hash helper (M11) | The default hash assertion is backed by a net-new `assert_reply_hash(source, *, expected_hash, event="agent_final")` helper in `easycat.debug.testing`, re-exported from `easycat.evals` (set A) so the generated `.py` imports it from `easycat.evals`. It hashes the (redacted) reply text with SHA-256 and raises `AssertionError` on mismatch. | `tests/test_public_api.py` (re-export set includes `assert_reply_hash`); `tests/evals/test_promote.py`. |
| Replay safety | Promoted replay denies tool execution by default. The `easycat.evals.replay_test.replay_bundle_as_test` helper replays at `ReplaySpec(fidelity=ARTIFACT, tool_policy=DENY)` unless `allow_tools=True`. | Replay-as-test tests; `tests/evals/test_package.py`. |
| Budgets package | `easycat.budgets.LatencyBudget` works and legacy import remains valid. | Budget alias tests. |
| Budgets submodule export set (M9) | `easycat.budgets` is a SUBMODULE package and does NOT count against the top-level `easycat.__all__` cap (94, unchanged). Its export set (`CostBudget`, `LatencyBudget`, `build_budget_report`, `BudgetReport`) is asserted EXPLICITLY in `tests/test_public_api.py`, and the net-new symbols (`CostBudget`/`build_budget_report`/`BudgetReport`) must NOT leak onto the top-level allowlist. | `tests/test_public_api.py::test_budgets_submodule_exports_are_public`. |
| Cost budget | `CostBudget` coexists with `max_session_cost_usd` alias. The net-new `@dataclass(frozen=True) CostBudget(max_session_usd, warn_at=0.8, action="warn"\|"stop")` exposes `max_session_cost_usd` as an alias property and round-trips via `to_dict`/`from_dict` (which accepts the alias key). | Config/budget tests (`tests/budgets/test_models.py`). |
| Runtime milestones | First-token/first-audio metrics are recorded where observable. | Session/runtime tests. |
| Runtime stage budgets (M12) | When a `LatencyBudget` is configured for it, a voice turn emits a flat per-stage runtime metric record (`stt_final_latency_ms`, `llm_ttft_ms`, `tts_ttfb_ms`, `first_audio_ms`) through `LatencyBudgetMonitor`; `tts_ttfb_ms`/`llm_ttft_ms` are RENAMES/lifts of the offline `validation/latency.py` columns (same measurement, not duplicates) and the four flat names are the runtime lift of the waterfall `*_to_*_ms` milestones. A milestone is recorded only when its budget exists and both timing markers were observed. | `tests/session/test_turn_runner.py::test_run_streaming_agent_records_stage_latency_milestones` + `::test_run_streaming_agent_skips_unbudgeted_stage_milestones`. |
| Runtime stage-name mapping (M12) | `session/_latency_budget._budget_matches_stage` matches a `LatencyBudget(stage=...)` against a runtime record whether the budget is expressed as the flat stage, the `_ms`/`_latency_ms` suffix form, or the waterfall milestone name that lifts onto the same flat stage (e.g. `vad_endpoint_to_stt_final_ms` ↦ `stt_final_latency_ms`). The M9 `build_budget_report`/offline percentile retrofit covers the same names. | `tests/session/test_latency_budget.py`. |
| Runtime budget-exceeded records (M12) | A breached per-stage budget appends a `latency_budget_exceeded` metric record (carrying `trigger_record_name` and the budget `stage`) to the journal/issue rollups, and tags the triggering metric with `latency_budget_exceeded`/`latency_budget_violations`. | `tests/session/test_turn_runner.py::test_run_streaming_agent_records_stage_budget_exceeded`; `tests/session/test_latency_budget.py::test_monitor_records_budget_exceeded_for_waterfall_named_budget`. |
| Shared report covers all three latency sources (M9) | `build_budget_report(records, budgets, *, percentiles=...)` evaluates ONE budget set across ALL THREE latency vocabularies: runtime `total_ms` single-observation records, waterfall `*_to_*_ms` milestone names (mapped onto flat budget stages), AND the offline validation percentile columns. The offline path is RETROFITTED onto `build_budget_report` (CONS-7) — `validation/latency.build_latency_artifact` routes its `budget_violations` through the shared builder while preserving the legacy `observed_ms`/`budget_ms` artifact shape. | `tests/budgets/test_report.py` (three sources + cost) and `tests/budgets/test_validation_retrofit.py` (offline path is the shared evaluator). |
| Shared report | Debugger, eval, CLI, and validation use shared budget report semantics. | Cross-surface tests. |
| Docs | Observability, evals, latency, validation, and teaching docs reflect new APIs. | Guard docs. |

## Global Acceptance Criteria

- New public APIs are documented and covered by public API tests.
- New CLIs support `--json` when they emit structured output.
- Optional dependencies remain optional and fail with actionable messages.
- No secrets or PII are introduced into metrics labels or unredacted planning output.
- Existing lower-level APIs remain available until a deliberate next-major
  removal/deprecation pass.
- Generated docs route files are regenerated when route maps change.
